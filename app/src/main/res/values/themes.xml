<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RoadAlertTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="colorAccent">@color/accent_color</item>
        <item name="android:textColorPrimary">@color/white</item>

        <!-- Link color theme-aware -->
        <item name="android:textColorLink">@color/my_light_linkcolor</item>

    </style>

    <style name="Theme.RoadAlertTracker" parent="Base.Theme.RoadAlertTracker" />
</resources>